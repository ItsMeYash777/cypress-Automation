name: Regression Tests
on:
  workflow_dispatch:

jobs:
  regression:
    runs-on: ubuntu-latest
    steps:
      - name: Wait for 2 mins
        run: sleep 120

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Cypress Tests
        run: echo "Running regression tests..."
          # Simulate Cypress tests here

      - name: Send Slack Notification
        run: echo "Slack notification sent with test results."
